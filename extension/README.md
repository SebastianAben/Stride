# Stride Browser Extension

Chrome Extension Manifest V3 implementation will live here.

Phase 8 will add the manifest, popup UI, active-tab metadata extraction, correction form, backend submit flow, and success/error states.
